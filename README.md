# docker-scrapy
> Docker for scrapy.

## commands
```shell
# build docker image
docker build -t scrapy:v1 .
# run docker container
docker-compose up -d
# login to docker container
docker exec -it scrapy /bin/bash
```